<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>E-Passport Portal</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    /* Sidebar */
    .sidebar {
      width: 220px;
      background-color: #fff;
      border-right: 1px solid #dee2e6;
      padding: 20px;
      position: fixed;
      top: 0;
      left: 0;
      height: 100%;
    }
    .sidebar-header img {
      display: block;
      margin: 0 auto 10px;
    }
    .sidebar-header h4 {
      margin: 0;
      font-weight: 600;
      color: #0d6efd;
    }
    .sidebar h4 { color: #0d6efd; margin-bottom: 1.5rem; font-weight: 600; }
    .sidebar a { display: block; color: #333; text-decoration: none; margin-bottom: 10px; font-size: 16px; }
    .sidebar a:hover { color: #0d6efd; }

    /* Main content */
    .main-content { margin-left: 220px; flex: 1; }

    /* Navbar */
    .navbar-custom { background-color: #0d6efd; }
    .navbar-custom .navbar-brand { color: #fff; font-weight: 500; }
    .navbar-custom .nav-link { color: #fff; font-weight: 500; }
    .navbar-custom .nav-link:hover { text-decoration: underline; }
  </style>
</head>
<body>
    <div class="sidebar">
      <div class="sidebar-header mb-3 text-center">
        <img src="/images/logo.jpg" alt="Logo" class="img-fluid mb-2" style="max-height: 60px;">
        <h4>Afghanistan General Directorate of Passports</h4>
      </div>
    <% if (!user) { %>
      <a href="/auth/login">Login</a>
      <a href="/auth/register">Register</a>
    <% } else { %>
      <% 
        let dashboardLink = '/';
        if (user.roles.includes('ADMIN')) dashboardLink = '/admin';
        else if (user.roles.includes('OFFICER')) dashboardLink = '/officer';
        else if (user.roles.includes('DEPT_HEAD')) dashboardLink = '/depthead';
        else if (user.roles.includes('CITIZEN')) dashboardLink = '/citizen';
      %>
      <a href="<%= dashboardLink %>">Dashboard</a>
      <a href="/profile">Profile</a>
      <a href="/auth/logout">Logout</a>
    <% } %>
  </div>

  <div class="main-content">
    <nav class="navbar navbar-expand-lg navbar-dark navbar-custom">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">E-Passport Portal</a>
        <div class="collapse navbar-collapse justify-content-end">
          <ul class="navbar-nav">
            <% if (!user) { %>
              <li class="nav-item"><a class="nav-link" href="/auth/login">Login</a></li>
              <li class="nav-item"><a class="nav-link" href="/auth/register">Register</a></li>
            <% } else { %>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                  <%= user.full_name %>
                  <% if(user.department_name) { %> - <%= user.department_name %> <% } %>
                </a>
                <ul class="dropdown-menu dropdown-menu-end">
                  <li>
                    <strong class="dropdown-item-text">Roles:</strong>
                    <span class="dropdown-item-text">
                      <%= (user.roles && user.roles.length) ? user.roles.join(", ") : "No roles assigned" %>
                    </span>
                  </li>
                  <li><hr class="dropdown-divider"></li>
                  <li><a class="dropdown-item" href="/profile">Profile</a></li>
                  <li><a class="dropdown-item" href="/auth/logout">Logout</a></li>
                </ul>
              </li>
            <% } %>
          </ul>
        </div>
      </div>
    </nav>
